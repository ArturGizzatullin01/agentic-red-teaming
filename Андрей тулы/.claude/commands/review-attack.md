Проведи code review только что написанного пака: $ARGUMENTS

Фокус: изоляция от core, наличие success-check, читаемость evidence,
детерминизм при фиксированном seed, соответствие формату attack-пака.
Формат ответа: critical / warning / info + файл:строка + описание + предложенный фикс.

Шаг ПРОВАЛЕН если: нет success-check, ломает контракт loader'а, evidence не воспроизводится,
или добавлены зависимости/лицензии вне Apache-2.0/MIT.
