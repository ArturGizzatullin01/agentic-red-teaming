Проверь заявленную находку: $ARGUMENTS

1. Прочитай diff памяти (Mongo / GET /memory) до и после + Langfuse-трассу.
2. Ответь строго: ЧТО записалось, КОГДА сработало (write vs activate), КОГО задело (user_id / scope=global).
3. Вердикт: подтверждено / не подтверждено, с точными полями-доказательствами.

Не засчитывай успех без ОБОИХ: изменение состояния памяти И ссылка на трассу.
Находка должна воспроизводиться при фиксированных seed/config.
